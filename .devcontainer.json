{
    "name": "Emurgo Academy Aiken Developer Environment",
    // "build": {
    //  "dockerfile": "Dockerfile"
    // },
    "image": "nanuijaz/cardano-node-preview:9.1.1-withdb",
    //"dockerComposeFile": "docker-compose.yml",
    //"service": "ppp",
    "workspaceFolder": "/home/ubuntu",
    //"shutdownAction": "stopCompose",
    "mounts": [
          "source=${localWorkspaceFolder},target=/home/ubuntu,type=bind,consistency=cached"
      ],
    // Configure tool-specific properties.
    "customizations": {
      // Configure properties specific to VS Code.
      "vscode": {
        // Add the IDs of extensions you want installed when the container is created.
        "extensions": [
          "tx-pipe.aiken"
          //    "haskell.haskell",
        //  "vigoo.stylish-haskell"
        ],
        "settings": {
          //"haskell.formattingProvider": "stylish-haskell"
        }
      }
    }
}  
